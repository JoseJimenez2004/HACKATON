{
  "name": "remesas-frontend",
  "version": "1.0.0",
  "scripts": {
    "dev": "live-server --port=3000",
    "build-css": "sass src/styles:public/css --watch",
    "start": "npm run build-css & npm run dev"
  },
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.66.1"
  }
}
